<h1>I need a new comment.
</h1>

<%= form_for @comment do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :content %>
    <%= f.text_field :content %>

    <%= f.submit "Submit" %>
  </div>

<% end %>